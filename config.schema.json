{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Config",
  "type": "object",
  "properties": {
    "banAppealForm": {
      "description": "Links to the ban appeal form",
      "type": "object",
      "properties": {
        "id": {
          "description": "The form ID",
          "type": "string",
          "minLength": 1
        },
        "questions": {
          "description": "Question IDs for various form questions",
          "type": "object",
          "properties": {
            "contactMethod": {
              "description": "Contact method question ID",
              "type": "string",
              "minLength": 1
            },
            "discordId": {
              "description": "Discord user ID question ID",
              "type": "string",
              "minLength": 1
            },
            "discordTag": {
              "description": "Discord tag question ID",
              "type": "string",
              "minLength": 1
            },
            "emailAddress": {
              "description": "Email address question ID",
              "type": "string",
              "minLength": 1
            },
            "twitterUsername": {
              "description": "Twitter username question ID",
              "type": "string",
              "minLength": 1
            }
          },
          "required": [
            "contactMethod",
            "discordId",
            "discordTag",
            "emailAddress",
            "twitterUsername"
          ]
        }
      },
      "required": [
        "id",
        "questions"
      ]
    },
    "bot": {
      "description": "Bot configuration",
      "type": "object",
      "properties": {
        "applicationId": {
          "description": "The Discord application ID",
          "type": "string",
          "pattern": "^\\d+$"
        }
      },
      "required": [
        "applicationId"
      ]
    },
    "guild": {
      "description": "Guild configuration",
      "type": "object",
      "properties": {
        "discussionChannel": {
          "description": "The channel used for discussing moderation actions",
          "type": "string",
          "pattern": "^\\d+$"
        },
        "errorChannel": {
          "description": "The channel to log errors in",
          "type": "string",
          "pattern": "^\\d+$"
        },
        "id": {
          "description": "The guild to register commands for",
          "type": "string",
          "pattern": "^\\d+$"
        },
        "privateChannels": {
          "description": "List of channels where bot replies are publicly visible",
          "type": "array",
          "items": [
            {
              "type": "string",
              "pattern": "^\\d+$"
            }
          ]
        },
        "restart": {
          "description": "Restart message configuration",
          "type": "object",
          "properties": {
            "channel": {
              "description": "The channel to send restart messages in",
              "type": "string",
              "pattern": "^\\d+$"
            },
            "user": {
              "description": "The user that is mentioned in restart messages",
              "type": "string",
              "pattern": "^\\d+$"
            }
          },
          "required": [
            "channel"
          ]
        },
        "warnCategory": {
          "description": "The category under which warning channels are created",
          "type": "string",
          "pattern": "^\\d+$"
        },
        "warnLogsChannel": {
          "description": "The channel to send warning logs in",
          "type": "string",
          "pattern": "^\\d+$"
        }
      },
      "required": [
        "discussionChannel",
        "errorChannel",
        "id",
        "privateChannels",
        "restart",
        "warnCategory",
        "warnLogsChannel"
      ]
    },
    "icons": {
      "description": "Embed author icon configuration",
      "type": "object",
      "properties": {
        "fail": {
          "description": "The embed author icon to use on fail",
          "type": "string",
          "format": "uri",
          "pattern": "^https://.+$"
        },
        "success": {
          "description": "The embed author icon to use on success",
          "type": "string",
          "format": "uri",
          "pattern": "^https://.+$"
        },
        "warning": {
          "description": "The embed author icon to use in the warning message",
          "type": "string",
          "format": "uri",
          "pattern": "^https://.+$"
        }
      },
      "required": [
        "fail",
        "success",
        "warning"
      ]
    },
    "repository": {
      "description": "GitHub repository configuration",
      "type": "object",
      "properties": {
        "owner": {
          "description": "The owner of the repository",
          "type": "string",
          "pattern": "^[A-z0-9-]+$"
        },
        "name": {
          "description": "The name of the repository",
          "type": "string",
          "pattern": "^[A-z0-9_.-]+$"
        }
      },
      "required": [
        "owner",
        "name"
      ]
    }
  },
  "required": [
    "banAppealForm",
    "bot",
    "guild",
    "icons",
    "repository"
  ]
}